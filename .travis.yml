language: java
jdk:
  - openjdk8

services:
  - xvfb

before_install:
  - sudo apt update
  - sudo apt install openjfx

install: true  

addons:
    apt:
      packages:
        ant
    sonarcloud:
      organization: bonjon
      token:
        secure: 8b6bb4377a19d3a1c963ef544d27c7b58ef80f7b
  
script: 
- ant -buildfile Cinefy/build.xml
- sonar-scanner
