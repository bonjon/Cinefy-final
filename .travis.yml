language: java
jdk:
  - openjdk8

services:
  - xvfb

before_install:
  - sudo apt update
  - sudo apt install openjfx

install: true  

addons:
    apt:
      packages:
        ant
    sonarcloud:
      organization: "bonjon"
      token:
        secure: "fa841c2da791a17ab28f60cd41b00420161027e2"

script: 
- ant -buildfile Cinefy/build.xml
- sonar-scanner
