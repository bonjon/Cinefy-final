language: java
jdk:
  - openjdk8

services:
  - xvfb

before_install:
  - sudo apt update
  - sudo apt install openjfx

install: true  

addons:
    apt:
      packages:
        ant
    sonarcloud:
      organization: bonjon
      token:
        secure: 6e8200684e5c57cb8d244c003787b9894dc5edac
  
script: 
- ant -buildfile Cinefy/build.xml
- sonar-scanner
