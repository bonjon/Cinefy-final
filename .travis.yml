language: java
jdk:
  - openjdk8

services:
  - xvfb

before_install:
  - sudo apt update
  - sudo apt install openjfx

install: true  

addons:
    apt:
      packages:
        ant
    sonarcloud:
      organization: bonjon
      token:
        secure: fcb6b92f4cc60f4bed3b4fa6b2c0139891da33e1

script: 
- ant -buildfile Cinefy/build.xml
- sonar-scanner
